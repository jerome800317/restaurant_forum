<div class="container">
  <div class="row">
    <% @users.each do |user| %>
      <li>
        <%= user.name %>
        <%= render partial: "shared/follow", locals: {user: user} %>
      </li>
    <% end %>
  </div>
</div>